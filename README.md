```$git add .
$git commit 
 - write your commit message
- esc
- :wq!

non mac user:
ctrl+X, 
Y
enter

$git push origin feature/user

go to github.com, create pull request (remember to change the base fork to your own repo)

create PR
merge PR

$ git checkout master
$ git pull origin master (let the latest code on github to update your local machine master branch)
```
